export type NavItem = {
    title: string
    url: string
}

export type Repository = {
    id: number
    githubId: number
    description: string
    link: string
    name: string
    position: number
    visible: boolean
}